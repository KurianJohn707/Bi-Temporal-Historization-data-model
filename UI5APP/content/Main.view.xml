<!-- this is a comment. in the next tag, the xmlns tags define 	
    namespaces, the name after of the equality signs being the name of
    the namespace. In the code below, we can use this name in-stead of
    the full path -->
<mvc:View controllerName="content.Main" 
    xmlns="sap.m" 
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:ux3="sap.ui.ux3"
    xmlns:table="sap.ui.table"
    xmlns:l="sap.ui.commons.layout"
    xmlns:html="http://www.w3.org/1999/xhtml">
    
	<!-- view content goes here -->
	
	<!-- with the first property, we're adding a small 
    	logo to our application. --> 
   <Page title="Title">		
		<customHeader>
		<Bar>
		<contentLeft>
    		<Image src="/sapucc/gbi/files/dev/logo.png" width="20px" height="20px"></Image>
    		<Text text = "BI-TEMPORAL HISTORIZATION DATA MODEL"></Text>
		</contentLeft>
		
	
		
		<contentRight>
		<Button text="Action" type="Accept"></Button>
		</contentRight>		
		</Bar>
		</customHeader>

	</Page>

    <!-- This is the text that we want to show -->
    <!-- here we have the definition of the table.
    Next to different settings for the appearance
    and behavior of the table, we bind the endpoint
    /Sales of our data source to our rows -->

    <html:p/>
    <table:Table
        id="salesOrdersTable"
        visibleRowCount="20"
        selectionMode="Single"
        navigationMode="Paginator"
        editable="false"
        rows="{/Technical_Table}">
        
        <!-- this defines one column in the table. The property
        "sortProperty" defines the column of the data source on
        which the column will sort -->
        <table:Column sortProperty="CUSTOMER_NUMBER" filterProperty="CUSTOMER_NUMBER">
            <!-- the label is shown in the header of the table -->
            <table:label>
                <Label text="Customer Number" design="Bold"/>
            </table:label>
            <!-- for each column, a template is required which
            defines how how the data is visualized -->
            <table:template>
                <!-- this label defines the actual content (aka the
                rows) that are shown in the table. here, we bind the
                column CUSTOMER_NAME of the data source
                to this table column -->
                <Label text="{CUSTOMER_NUMBER}"/>
            </table:template>
        </table:Column>
        
        <table:Column sortProperty="CUSTOMER_NAME" filterProperty="CUSTOMER_NAME">
            <table:label>
                <Label text="Name" design="Bold"/>
            </table:label>
            <table:template>
                <Label text="{CUSTOMER_NAME}"/>
            </table:template>
        </table:Column>
        
        <table:Column sortProperty="CITY" filterProperty="CITY">
            <Label text="City" design="Bold"/>
            <table:template>
                <Label text="{CITY}"/>
            </table:template>
        </table:Column>
        
        <table:Column sortProperty="MODIFICATION_TIME" >
            <table:label>
                <Label text="Modification Time" design="Bold"/>
            </table:label>
            <table:template>
                <Label text="{MODIFICATION_TIME}"/>
            </table:template>
        </table:Column>
        
        <table:Column sortProperty="BUSINESS_VALID_FROM">
            <table:label>
                <Label text="Business Valid From" design="Bold"/>
            </table:label>
            <table:template>
                <Label text="{BUSINESS_VALID_FROM}"/>
            </table:template>
        </table:Column>
        
        <table:Column sortProperty="BUSINESS_VALID_TO">
            <table:label>
                <Label text="Business Valid To" design="Bold"/>
            </table:label>
            <table:template>
                <Label text="{BUSINESS_VALID_TO}"/>
            </table:template>
        </table:Column>
        
        <table:Column sortProperty="TECHNICAL_VALID_FROM">
            <table:label>
                <Label text="Technical Valid From" design="Bold"/>
            </table:label>
            <table:template>
                <Label text="{TECHNICAL_VALID_FROM}"/>
            </table:template>
        </table:Column>
        
        <table:Column sortProperty="TECHNICAL_VALID_TO">
            <table:label>
                <Label text="Technical Valid To" design="Bold"/>
            </table:label>
            <table:template>
                <Label text="{TECHNICAL_VALID_TO}"/>
            </table:template>
        </table:Column>
        
    </table:Table>

    
    
</mvc:View>
